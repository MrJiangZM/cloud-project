server:
  port: 7001

eureka:
  instance:
    hostname: eurekaServer # eureka 服务端实例名
  client:
    register-with-eureka: false # 不向注册中心注册自己 单机使用
    fetch-registry: false # 表示自己是注册中心，职责就是维护服务实例
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/